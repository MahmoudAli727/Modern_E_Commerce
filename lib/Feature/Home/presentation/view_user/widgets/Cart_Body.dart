import 'package:flutter/cupertino.dart';

class Cart_Body extends StatelessWidget {
  const Cart_Body({super.key});
  static String routeName = "/Cart_Body";

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
